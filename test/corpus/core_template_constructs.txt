========================
Include tag (deprecated)
========================

{% include 'hello.liquid' %}

---

(template
  (content)
  (directive
    (include_expression file: (string))
  )
  (content)
)

========================
Render tag
========================

{% render 'hello.liquid' %}

---

(template
  (content)
  (directive
    (render_expression file: (string))
  )
  (content)
)

========================
for loop
========================

{% for item in sequence %}
  <li>{{item}}</li>
{% endfor %}

---

(template
  (content)
  (for_block
    (for_directive
      left: (identifier)
      right: (identifier)
    )
    (content)
    (output_directive (identifier))
    (content)
    (endfor_directive)
  )
  (content)
)
